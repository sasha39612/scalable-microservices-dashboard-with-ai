apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  NODE_ENV: "development"
  NEXT_PUBLIC_API_URL: "http://api-gateway:4000/graphql"
  JWT_SECRET: "your-jwt-secret-key"
  JWT_EXPIRATION: "24h"